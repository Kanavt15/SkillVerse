{
  "name": "@skillverse/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Code shared by the API and the web app: validation schemas, money helpers, IDs, constants.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "zod": "^4.3.3"
  },
  "devDependencies": {
    "vitest": "~4.1.11"
  }
}
